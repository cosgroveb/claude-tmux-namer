{
  "description": "Rename tmux session after each Claude response using Haiku",
  "hooks": {
    "Stop": [
      {
        "type": "command",
        "command": "${CLAUDE_PLUGIN_ROOT}/scripts/tmux-namer.zsh",
        "timeout": 2
      }
    ]
  }
}
